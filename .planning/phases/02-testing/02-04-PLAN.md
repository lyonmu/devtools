---
phase: 02-testing
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - src/cert/mod.rs
  - src/cert/fixtures/README.md
  - src/cert/fixtures/sample.pem
  - src/cert/fixtures/sample.der
  - src/cert/fixtures/chain.pem
  - src/cert/fixtures/sample.p12
autonomous: true
requirements: [R5.1, R5.3, R5.4]
must_haves:
  truths:
    - "Certificate parser tests use checked-in, small, non-secret fixtures for every supported detect_and_parse format."
    - "PEM, DER, multi-PEM, and PKCS#12 parsing paths are covered with stable inline tests."
    - "PKCS#12 test material documents `test-password` as public/non-secret per D-13."
  artifacts:
    - path: "src/cert/fixtures/"
      provides: "Non-secret X.509 test fixture set"
      contains: "README.md"
    - path: "src/cert/mod.rs"
      provides: "Inline parser/detect_and_parse fixture tests"
      contains: "include_bytes!(\"fixtures/"
  key_links:
    - from: "src/cert/mod.rs tests"
      to: "src/cert/fixtures/*"
      via: "include_bytes! and temp-file detect_and_parse coverage"
      pattern: "include_bytes!"
---

<objective>
Create certificate fixtures and cover certificate format parsing.

Purpose: D-07 prioritizes certificate tests before PQ modules, and D-10 through D-13 require realistic checked-in fixtures for PEM, DER, multi-PEM, and PKCS#12 with no secrets.
Output: Fixture directory plus inline tests for certificate parser entry points.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-testing/02-CONTEXT.md
@.planning/phases/02-testing/02-03-SUMMARY.md
@AGENTS.md
@src/cert/mod.rs

<interfaces>
From `src/cert/mod.rs`:
```rust
pub fn parse_der_cert(bytes: &[u8], path: &str) -> Result<ParsedCert, String>;
pub fn parse_pem_cert(pem_data: &[u8], path: &str) -> Result<ParsedCert, String>;
pub fn parse_pkcs12(bytes: &[u8], password: &str, path: &str) -> Result<Vec<ParsedCert>, String>;
pub fn detect_and_parse(path: &Path) -> Result<Vec<ParsedCert>, String>;
pub fn parse_pem_multi(pem_data: &[u8], path: &str) -> Result<Vec<ParsedCert>, String>;
```
D-10/D-11/D-12/D-13 fixture contract:
- Fixtures live under `src/cert/fixtures/`.
- Tests reference them with stable paths such as `include_bytes!("fixtures/sample.pem")`.
- Coverage includes PEM, DER, multi-PEM, and PKCS#12.
- PKCS#12 password is public/non-secret `test-password` and documented next to fixtures or in test code.
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add non-secret certificate fixtures</name>
  <files>src/cert/fixtures/README.md, src/cert/fixtures/sample.pem, src/cert/fixtures/sample.der, src/cert/fixtures/chain.pem, src/cert/fixtures/sample.p12</files>
  <action>Create `src/cert/fixtures/` and generate small self-signed test-only certificate material for parser tests per D-10 through D-13. Use public/non-secret material only. Prefer local OpenSSL generation if available: create a temporary RSA key/cert, convert PEM to DER, create `chain.pem` by concatenating two public test certs, and export `sample.p12` with password `test-password`; delete any temporary private key after fixture generation. If OpenSSL is unavailable, create equivalent checked-in public fixture bytes by another local tool, but do not include real production certs or private credentials. Document in `README.md` that fixtures are public, generated for tests, and `test-password` is not secret.</action>
  <verify>
    <automated>test -f src/cert/fixtures/sample.pem && test -f src/cert/fixtures/sample.der && test -f src/cert/fixtures/chain.pem && test -f src/cert/fixtures/sample.p12 && grep -v '^#' src/cert/fixtures/README.md | grep -q "test-password"</automated>
  </verify>
  <done>All required fixture formats exist under `src/cert/fixtures/` and are documented as non-secret test material.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add parser fixture tests in cert module</name>
  <files>src/cert/mod.rs</files>
  <behavior>
    - `parse_pem_cert` and `parse_der_cert` parse sample fixtures and populate subject, issuer, serial, version, signature algorithm, public key info, and raw path.
    - `parse_pem_multi` returns multiple certificates from `chain.pem`.
    - `parse_pkcs12` succeeds with `test-password` and fails with a wrong password.
    - `detect_and_parse` covers `.pem`, `.der`, `.crt`/`.cer`, `.p12`/`.pfx`, unsupported extension, and invalid bytes paths.
  </behavior>
  <action>Extend the inline tests in `src/cert/mod.rs` per D-07 and D-12. Use `include_bytes!("fixtures/...")` for fixture input and temporary files under `std::env::temp_dir()` for `detect_and_parse` extension routing. Ensure temp filenames are unique enough for parallel cargo test runs. For PKCS#12 direct parsing use `test-password`; for current `detect_and_parse` `.p12/.pfx` behavior, assert the documented password-required error if the parser calls `parse_pkcs12` with an empty password.</action>
  <verify>
    <automated>cargo test -p devtools -- cert</automated>
  </verify>
  <done>Certificate parser tests cover all supported input formats and representative success/error routes using checked-in fixtures.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checked-in fixtures → repository | Certificate fixture files could accidentally contain secrets. |
| file extension/input bytes → parser dispatch | Untrusted local files cross into DER/PEM/PKCS#12 parsing logic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04-01 | Information Disclosure | src/cert/fixtures | mitigate | Use generated public test certs only, document `test-password`, and delete temporary private keys. |
| T-02-04-02 | Denial of Service | detect_and_parse | mitigate | Add invalid/unsupported file tests asserting controlled errors. |
</threat_model>

<verification>
Run `cargo test -p devtools -- cert` and `cargo test -p devtools`.
</verification>

<success_criteria>
Certificate parsing has realistic fixture-backed coverage for PEM, DER, multi-PEM, and PKCS#12 without adding production secrets or external service setup.
</success_criteria>

<output>
After completion, create `.planning/phases/02-testing/02-04-SUMMARY.md`
</output>
