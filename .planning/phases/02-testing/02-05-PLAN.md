---
phase: 02-testing
plan: 05
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - src/cert/extensions.rs
  - src/cert/oid_resolver.rs
autonomous: true
requirements: [R5.1, R5.4]
must_haves:
  truths:
    - "Certificate extension display helpers have boundary and known-value tests."
    - "OID resolution covers classic, GM/T, PQ, extension, unknown, and key-category boundary behavior."
  artifacts:
    - path: "src/cert/extensions.rs"
      provides: "Inline tests for extension formatting/parsing helpers"
      contains: "#[cfg(test)]"
    - path: "src/cert/oid_resolver.rs"
      provides: "Expanded inline tests for OID maps and category boundaries"
      contains: "#[cfg(test)]"
  key_links:
    - from: "src/cert/extensions.rs tests"
      to: "parse_extension_value"
      via: "known OID value-display assertions"
      pattern: "parse_extension_value"
    - from: "src/cert/oid_resolver.rs tests"
      to: "resolve_algorithm_name/resolve_extension_name/resolve_key_category"
      via: "map lookup and prefix-boundary assertions"
      pattern: "resolve_key_category"
---

<objective>
Cover certificate extension and OID helper logic in parallel with parser fixture work.

Purpose: D-07 makes `src/cert/*` a core priority before PQ modules; this plan covers cert helper files that do not share file ownership with parser fixture tests.
Output: Expanded inline tests for extension formatting and OID resolution.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-testing/02-CONTEXT.md
@.planning/phases/02-testing/02-03-SUMMARY.md
@AGENTS.md
@src/cert/extensions.rs
@src/cert/oid_resolver.rs

<interfaces>
From `src/cert/extensions.rs`:
```rust
pub struct CertExtension { pub oid: String, pub name: String, pub critical: bool, pub value_display: String }
pub fn parse_extensions(x509: &X509Certificate<'_>) -> Vec<CertExtension>;
```
Private helpers are in-module and testable from inline tests: `format_hex`, `parse_basic_constraints`, `parse_key_usage`, `parse_subject_alt_name`, `parse_subject_key_identifier`, and `parse_extension_value`.
From `src/cert/oid_resolver.rs`:
```rust
pub enum KeyCategory { Classic, PostQuantum }
pub fn resolve_algorithm_name(oid: &str) -> String;
pub fn resolve_key_category(oid: &str) -> KeyCategory;
pub fn resolve_extension_name(oid: &str) -> String;
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add extension helper boundary tests</name>
  <files>src/cert/extensions.rs</files>
  <behavior>
    - Empty and arbitrary bytes format as lowercase spaced hex.
    - Basic Constraints identifies CA true/false and falls back for too-short input.
    - Key Usage identifies multiple flags and falls back when no flags are present.
    - Subject Alternative Name extracts printable names and falls back to hex when none exist.
    - Subject Key Identifier strips OCTET STRING wrapper only for the expected 22-byte form.
  </behavior>
  <action>Add an inline `#[cfg(test)] mod tests` to `src/cert/extensions.rs`. Test private helper behavior directly because the helpers are in the same module and this file has no existing tests. Cover all listed OID branches in `parse_extension_value`, plus unknown OID fallback. Do not create synthetic X509Certificate values; parser-level extension extraction is covered by Plan 04 fixtures.</action>
  <verify>
    <automated>cargo test -p devtools -- cert::extensions</automated>
  </verify>
  <done>Extension display helpers have deterministic tests for known OID branches, fallback formatting, and empty/boundary input.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand OID resolver mapping and category tests</name>
  <files>src/cert/oid_resolver.rs</files>
  <behavior>
    - Classic RSA/ECDSA/SHA/AES OIDs resolve to exact names.
    - GM/T SM2/SM3/SM4 and PQ ML-KEM/ML-DSA OIDs resolve to exact names.
    - Extension OIDs resolve to exact names and unknown extension OIDs use the Chinese unknown format.
    - PQ category prefix boundaries classify intended PQ arcs as PostQuantum and adjacent non-PQ arcs as Classic.
  </behavior>
  <action>Extend the existing inline tests in `src/cert/oid_resolver.rs` per D-07. Add exact-name tests for representative classic, GM/T, PQ, and extension mappings already present in the static maps. Add boundary tests for `resolve_key_category` so adjacent unknown OIDs do not accidentally classify as PQ. Keep tests deterministic and table-driven where concise.</action>
  <verify>
    <automated>cargo test -p devtools -- cert::oid_resolver</automated>
  </verify>
  <done>OID resolver tests cover map lookups, unknown fallbacks, and key-category prefix boundaries.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| certificate extension bytes → display helpers | Malformed extension bytes are converted for display. |
| OID strings → category/name mapping | Untrusted certificate OIDs influence displayed algorithm category. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Tampering | OID category resolver | mitigate | Add prefix-boundary tests so adjacent non-PQ OIDs do not get misclassified. |
| T-02-05-02 | Denial of Service | extension helper parsers | mitigate | Test empty, short, unknown, and malformed byte inputs return display strings without panics. |
</threat_model>

<verification>
Run `cargo test -p devtools -- cert::extensions`, `cargo test -p devtools -- cert::oid_resolver`, and `cargo test -p devtools -- cert`.
</verification>

<success_criteria>
Certificate helper coverage supports the Phase 2 core coverage gate without depending on UI or broader certificate-chain feature work.
</success_criteria>

<output>
After completion, create `.planning/phases/02-testing/02-05-SUMMARY.md`
</output>
