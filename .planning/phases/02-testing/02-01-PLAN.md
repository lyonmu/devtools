---
phase: 02-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/02-testing/COVERAGE-WAIVERS.md
autonomous: true
requirements: [R5.1, R5.3, R5.4]
must_haves:
  truths:
    - "Executor can run the required Phase 2 coverage command and obtain measured evidence for core non-GPUI modules."
    - "Coverage exceptions are impossible to accept silently because every waiver has a named file/function, reason, missing tests, and follow-up phase per D-04."
  artifacts:
    - path: ".planning/phases/02-testing/COVERAGE-WAIVERS.md"
      provides: "Auditable waiver ledger for hard-to-measure coverage gaps"
      contains: "No active waivers"
  key_links:
    - from: "Phase 2 verification"
      to: "cargo tarpaulin -p devtools --out Html"
      via: "required coverage gate per D-03"
      pattern: "cargo tarpaulin -p devtools --out Html"
---

<objective>
Establish the Phase 2 coverage verification harness before adding tests.

Purpose: D-01 and D-03 make measured coverage the hard completion gate, so executors need tarpaulin availability, a baseline command, and waiver discipline before modifying test-heavy modules.
Output: tarpaulin setup verified, baseline coverage captured, waiver ledger created.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-testing/02-CONTEXT.md
@AGENTS.md
@Cargo.toml

<interfaces>
Phase 2 hard decisions:
- D-01: measured line coverage must be >80% for core non-GPUI modules before completion.
- D-02: hard gate applies only to src/algo/* and src/cert/*; GPUI files are excluded.
- D-03: required verification command is `cargo tarpaulin -p devtools --out Html`; install cargo-tarpaulin if missing.
- D-04: waivers must name file/function, reason, missing tests, and follow-up phase; ordinary untested branches cannot be waived.
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Verify tarpaulin availability and baseline command</name>
  <files>.planning/phases/02-testing/COVERAGE-WAIVERS.md</files>
  <action>Check whether `cargo tarpaulin --version` works. If missing, install the tool with `cargo install cargo-tarpaulin` per D-03, then run `cargo test -p devtools` and `cargo tarpaulin -p devtools --out Html` to capture the baseline. Do not add CI, lint, format, or benchmark tooling because AGENTS.md forbids unrequested infrastructure and the deferred Phase 2 idea excludes broader benchmarking.</action>
  <verify>
    <automated>cargo test -p devtools</automated>
    <automated>cargo tarpaulin -p devtools --out Html</automated>
  </verify>
  <done>Required coverage command runs locally and produces tarpaulin output/HTML evidence without changing project build configuration.</done>
</task>

<task type="auto">
  <name>Task 2: Create waiver ledger with hard-gate rules</name>
  <files>.planning/phases/02-testing/COVERAGE-WAIVERS.md</files>
  <action>Create `.planning/phases/02-testing/COVERAGE-WAIVERS.md` with a short Phase 2 policy section referencing D-01 through D-04, an initial `No active waivers` status, and a required waiver template containing: file, function/branch, reason, missing tests, evidence command, and follow-up phase. State that randomness-heavy or platform/tooling limitations may be waived only when specifically named; ordinary missing branches must be covered by tests.</action>
  <verify>
    <automated>test -f .planning/phases/02-testing/COVERAGE-WAIVERS.md && grep -v '^#' .planning/phases/02-testing/COVERAGE-WAIVERS.md | grep -q "No active waivers"</automated>
  </verify>
  <done>Waiver ledger exists and prevents undocumented exceptions to the >80% core coverage gate.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local tooling → coverage evidence | Tarpaulin output influences whether Phase 2 is accepted. |
| executor → waiver ledger | Executor-written waivers could hide missing test coverage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | COVERAGE-WAIVERS.md | mitigate | Require explicit D-04 fields and start with `No active waivers`; final plan must re-check ordinary gaps are tested. |
| T-02-01-02 | Repudiation | coverage command evidence | mitigate | Run `cargo test -p devtools` and `cargo tarpaulin -p devtools --out Html` as automated verification and record results in the summary. |
</threat_model>

<verification>
Run `cargo test -p devtools` and `cargo tarpaulin -p devtools --out Html`. Confirm no project CI/lint/format configuration was added.
</verification>

<success_criteria>
Tarpaulin is usable, the required command has baseline evidence, and waiver rules exist before any test expansion starts.
</success_criteria>

<output>
After completion, create `.planning/phases/02-testing/02-01-SUMMARY.md`
</output>
