---
phase: 04-ui-ux-bug-fixes
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/app.rs
  - src/algo/symmetric.rs
  - src/algo/asymmetric.rs
  - src/algo/hash.rs
autonomous: true
requirements:
  - FR-4
  - FR-5
  - NFR-1
must_haves:
  truths:
    - "D-01: The app keeps a fixed top tab bar, fixed left menu, and right-side tool work area."
    - "D-02: Tool pages render form/input area above results."
    - "D-03: Typography, line height, spacing, and section hierarchy prioritize readability."
    - "D-06: The dark theme remains, with normalized contrast/status colors/spacing."
    - "D-05: Result cards and panels use subtle boundaries/backgrounds, minimal visual noise, and no heavy dashboard-style shadows."
    - "D-07/D-12: Empty, success, error, execute, reset, and copy feedback appears through a top status/banner plus detailed result area."
    - "D-08: UI refactoring stays inside Phase 4 scope and extracts/reuses layout/input/button/card/scroll patterns without adding theme switching or collapsible navigation."
    - "D-09/D-10/D-11/D-13/D-15: Controls use compact Execute then Reset groups across certificate and algorithm tabs; reset clears inputs, outputs, and status/errors; invalid/missing inputs show clear errors after click."
  artifacts:
    - path: "src/app.rs"
      provides: "Shared workbench layout, status banner, compact action row, and result-card helpers"
      contains: "render_status_banner"
    - path: "src/algo/symmetric.rs"
      provides: "Reset behavior preserving selected symmetric algorithm/mode"
      contains: "pub fn reset"
    - path: "src/algo/asymmetric.rs"
      provides: "Reset behavior preserving selected asymmetric operation/key-size"
      contains: "pub fn reset"
    - path: "src/algo/hash.rs"
      provides: "Reset behavior preserving selected hash algorithm/input format"
      contains: "pub fn reset"
  key_links:
    - from: "src/app.rs"
      to: "src/algo/symmetric.rs"
      via: "compact action row reset calls symmetric.reset()"
      pattern: "symmetric\.reset"
    - from: "src/app.rs"
      to: "src/algo/asymmetric.rs"
      via: "compact action row reset calls asymmetric.reset()"
      pattern: "asymmetric\.reset"
    - from: "src/app.rs"
      to: "src/algo/hash.rs"
      via: "compact action row reset calls hash.reset()"
      pattern: "hash\.reset"
---

<objective>
Normalize the workbench layout, typography, status feedback, and operation controls across existing certificate and algorithm surfaces.

Purpose: The phase is not only an input bug fix; user decisions D-01 through D-16 require a production-grade workbench layout, readable typography, compact control groups, reset behavior, and clear validation/status feedback across the existing app.
Output: Shared layout/control/status helpers in `src/app.rs` and reset methods in algorithm state files that executors can use consistently.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-ui-ux-bug-fixes/04-CONTEXT.md
@.planning/phases/04-ui-ux-bug-fixes/04-RESEARCH.md
@.planning/phases/04-ui-ux-bug-fixes/04-VALIDATION.md
@.planning/phases/04-ui-ux-bug-fixes/04-01-SUMMARY.md
@AGENTS.md
@src/app.rs
@src/algo/symmetric.rs
@src/algo/asymmetric.rs
@src/algo/hash.rs

<interfaces>
Existing state methods:
```rust
this.algo_mut().symmetric.execute();
this.algo_mut().asymmetric.execute();
this.algo_mut().hash.compute();
this.algo_mut().pq_kem.keygen();
this.algo_mut().pq_kem.clear();
this.algo_mut().pq_signature.keygen();
this.algo_mut().pq_signature.clear();
```
Required reset methods for non-PQ states:
```rust
pub fn reset(&mut self) { /* clear inputs, outputs, status/errors; preserve selected algorithm/mode/op */ }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reset semantics and validation-preserving execution helpers</name>
  <files>src/algo/symmetric.rs, src/algo/asymmetric.rs, src/algo/hash.rs</files>
  <read_first>
    - src/algo/symmetric.rs
    - src/algo/asymmetric.rs
    - src/algo/hash.rs
    - .planning/phases/04-ui-ux-bug-fixes/04-CONTEXT.md
  </read_first>
  <behavior>
    - `SymmetricToolState::reset()` clears `input_hex`, `key_hex`, `iv_hex`, `output_hex`, and `error`, while preserving `selected_algo` and `mode`.
    - `AsymmetricToolState::reset()` clears `input_text`, `output_text`, `error`, `rsa_pub_key_pem`, `rsa_priv_key_pem`, `signature_hex`, `verify_result`, `ecc_pub_key_hex`, and `ecc_priv_key_hex`, while preserving `selected_op` and `rsa_key_size`.
    - `HashToolState::reset()` clears `input_text`, `output_hex`, and `error`, while preserving `selected_algo` and `input_format`.
    - Invalid/missing input still flows through execute/compute and sets a Chinese error message; buttons are not disabled solely because input is missing.
  </behavior>
  <action>
    Add or correct inline `#[cfg(test)]` tests in each touched algorithm file for reset behavior. Then implement/correct the public `reset()` methods to match the exact behavior above per D-11 and D-15.

    Do not clear selected algorithm/mode/operation/format in reset methods. Do not change crypto algorithms. Preserve existing Chinese error strings or replace only with clearer Chinese validation text.
  </action>
  <verify>
    <automated>cargo test -p devtools -- algo:: && cargo build -p devtools</automated>
  </verify>
  <acceptance_criteria>
    - `src/algo/symmetric.rs` contains `pub fn reset(&mut self)`.
    - `src/algo/asymmetric.rs` contains `pub fn reset(&mut self)`.
    - `src/algo/hash.rs` contains `pub fn reset(&mut self)`.
    - `src/algo/symmetric.rs` contains a reset test that asserts `selected_algo` and `mode` are preserved.
    - `src/algo/asymmetric.rs` contains a reset test that asserts `selected_op` and `rsa_key_size` are preserved.
    - `src/algo/hash.rs` contains a reset test that asserts `selected_algo` and `input_format` are preserved.
    - `cargo test -p devtools -- algo:: && cargo build -p devtools` exits 0.
  </acceptance_criteria>
  <done>Reset behavior is explicit, tested, and preserves user-selected algorithm/mode state while clearing inputs/results/status/errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create shared status, typography, card, and compact action controls</name>
  <files>src/app.rs</files>
  <read_first>
    - src/app.rs
    - src/algo/symmetric.rs
    - src/algo/asymmetric.rs
    - src/algo/hash.rs
    - .planning/phases/04-ui-ux-bug-fixes/04-CONTEXT.md
  </read_first>
  <action>
    In `src/app.rs`, add shared UI helpers and use them across existing certificate/algorithm surfaces:

    1. Typography constants must remain exactly `FONT_TITLE = gpui::px(18.0)`, `FONT_BODY = gpui::px(16.0)`, and `FONT_SMALL = gpui::px(14.0)`. Replace remaining `text_lg`, `text_sm`, and `text_xs` calls in `src/app.rs` with these constants.
    2. Add `enum UiStatusKind { Empty, Success, Error, Info }` and `fn render_status_banner(kind: UiStatusKind, message: impl Into<String>) -> gpui::Div`. Use dark-theme colors: info `rgb(0x3b82f6)`, success `rgb(0x22c55e)`, error `rgb(0xf87171)`, empty/neutral `rgb(0x888899)`.
    3. Add `fn action_button(id: &str, label: &str, color: gpui::Hsla, on_click: impl ...)` or an equivalent helper that produces compact buttons. If Rust closure typing makes a generic helper impractical, use a local helper that returns styled button shells and attach listeners at call sites.
    4. Add `fn action_row(primary, reset) -> gpui::Div` or equivalent repeated row pattern so buttons are compact, side-by-side, and ordered `执行` first then `重置` per D-09/D-10.
    5. Add `fn result_card(title: &str, body: gpui::AnyElement) -> gpui::Div` using subtle dark boundaries/backgrounds and no heavy shadows per D-05.
    6. Wire execute/reset rows for symmetric, asymmetric, hash, PQ KEM, and PQ signature tools. Non-PQ reset buttons call the new `reset()` methods; PQ reset buttons call existing `clear()` methods.
    7. Show a status banner above tool details for empty/success/error states. Examples: `请选择输入并点击执行`, `执行完成`, `错误: ...`. Keep detailed output in result cards below the form.

    Apply D-13 across certificate and algorithm tabs: certificate import button and status messages should use the same typography/button/status visual language even though certificate parsing has different actions. Do not add deferred features.
  </action>
  <verify>
    <automated>cargo build -p devtools && cargo test -p devtools</automated>
  </verify>
  <acceptance_criteria>
    - `src/app.rs` contains `render_status_banner`.
    - `src/app.rs` contains `UiStatusKind`.
    - `src/app.rs` contains `FONT_TITLE` with `px(18.0)`, `FONT_BODY` with `px(16.0)`, and `FONT_SMALL` with `px(14.0)`.
    - `src/app.rs` contains button labels `执行` and `重置`.
    - `src/app.rs` contains calls to `symmetric.reset()`, `asymmetric.reset()`, `hash.reset()`, `pq_kem.clear()`, and `pq_signature.clear()`.
    - `src/app.rs` contains no `text_lg`, `text_sm`, or `text_xs` tokens outside comments.
    - `cargo build -p devtools && cargo test -p devtools` exits 0.
  </acceptance_criteria>
  <done>Workbench layout/control visuals are consistent, readable, and use compact execute/reset groups with top status feedback and low-emphasis result panels.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User click → crypto tool action | User-triggered execution/reset can parse local input and mutate output state. |
| Tool state → UI status/result cards | Errors and outputs are displayed locally in the desktop app. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | Reset methods | mitigate | Inline tests verify reset clears sensitive outputs and preserves only selected mode/algorithm controls. |
| T-04-05 | Repudiation | Execute/reset feedback | accept | Local desktop tool has no audit trail requirement; status banner makes immediate outcome visible to the user. |
| T-04-06 | Information Disclosure | Result cards | accept | Pure local processing; Plan 03 adds explicit copy behavior and visual target clarity. |
</threat_model>

<verification>
Run `cargo test -p devtools -- algo:: && cargo build -p devtools` after reset changes, then `cargo build -p devtools && cargo test -p devtools` after UI helper wiring. Manual UAT should confirm buttons are compact, ordered Execute then Reset, and invalid input shows a clear Chinese error after clicking Execute.
</verification>

<success_criteria>
- D-01 through D-13 and D-15 are reflected in layout/control/status code.
- Reset behavior clears current page/tool state without clearing selected algorithm/mode/format.
- Buttons are compact groups, not full-width action bars.
- Build and tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/04-ui-ux-bug-fixes/04-02-SUMMARY.md`.
</output>
